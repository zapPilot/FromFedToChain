name: fromfedtochain_v2
description: Simplified audio streaming app for crypto/macro economics content
publish_to: 'none'

version: 2.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Audio Playback (3)
  just_audio: ^0.9.36
  audio_service: ^0.18.12
  audio_session: ^0.1.18

  # State Management & HTTP (3)
  provider: ^6.1.1
  dio: ^5.4.0
  shared_preferences: ^2.2.2

  # Authentication (2)
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.1.1

  # UI Utilities (2)
  shimmer: ^3.0.0
  cached_network_image: ^3.3.1

  # Utilities (3)
  equatable: ^2.0.5
  intl: ^0.19.0
  flutter_dotenv: ^5.1.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/images/
    - assets/icons/

  # Fonts commented out - using system fonts
  # To enable custom fonts, add Poppins font files to assets/fonts/
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/Poppins-SemiBold.ttf
  #         weight: 600
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700
