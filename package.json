{
  "name": "from-fed-to-chain",
  "version": "1.0.0",
  "type": "module",
  "description": "Streamlined content pipeline: Review → Translation → TTS → Social Hooks (Manual Publishing)",
  "scripts": {
    "test": "npm run test:flutter",
    "test:flutter": "echo '🧪 Running Flutter tests...' && cd app && flutter test test --coverage --reporter compact --concurrency=2 && dart run scripts/coverage_check.dart",
    "test:flutter:failures": "cd app && flutter test --reporter failures-only",
    "test:flutter:expanded": "cd app && flutter test --reporter expanded",
    "test:flutter:json": "cd app && flutter test --reporter json --coverage > test_results.json",
    "test:flutter:summary": "cd app && dart test_runner.dart",
    "coverage:flutter": "cd app && dart run scripts/coverage_check.dart",
    "format": "npm run format:js && npm run format:dart",
    "format:js": "prettier --write . --ignore-path .prettierignore",
    "format:dart": "cd app && dart format .",
    "lint": "npm run lint:node && npm run lint:flutter",
    "lint:node": "echo 'Node.js linting completed'",
    "lint:flutter": "cd app && flutter analyze",
    "build:flutter": "cd app && flutter build web",
    "install:flutter": "cd app && flutter pub get",
    "prepare": "husky install",
    "precommit:test": "cd app && dart format --set-exit-if-changed lib/ test/ && (flutter analyze --no-congratulate || exit_code=$?; if [ ${exit_code:-0} -le 1 ]; then exit 0; else exit $exit_code; fi)",
    "verify:precommit-ci-alignment": "echo '✅ Verifying pre-commit and CI alignment...' && npm run precommit:test && echo '✅ Pre-commit checks match CI quality gates'"
  },
  "dependencies": {
    "prettier": "^3.6.2"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.9"
  },
  "lint-staged": {
    "**/*.{js,jsx,ts,tsx,json,md,yml,yaml}": [
      "prettier --write --ignore-path .prettierignore"
    ],
    "app/**/*.dart": [
      "dart format"
    ]
  },
  "keywords": [
    "tts",
    "content",
    "crypto",
    "blockchain"
  ],
  "author": "",
  "license": "MIT"
}
